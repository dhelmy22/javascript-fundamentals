

// FAT ARROW FUNCTION

let fatArrow = () => {
    console.log('I reside the new way of writing functions.');
}

fatArrow();
// -------------------------------------------------------------------------------
let conciseBody = () => console.log('I am consice');

conciseBody();
// ------------concise body only allows a single statement in the function body-------------------------------

let blockBody = () => {
    let y = 0;
    y += 4;
    console.log(y);
}
console.log(y);
//  block body allows for multi line statements ----------------------------

